package com.example.seachem_dosing.ui.theme

import androidx.compose.material3.Typography

// Material 3 default type scale — matches the textAppearance* names the XML used
// (headlineMedium, titleMedium, bodyMedium, bodySmall). Customise here if needed.
val AppTypography = Typography()
